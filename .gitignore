# Build outputs
os.img
bootloader/boot
kernel/kernel
kernel/kernel.elf
kernel/build/

programs/build

# compile_commands.json is a symlink into kernel/build/, generated by
# CMake every configure - not meant to be committed, just regenerated.
kernel/compile_commands.json

# Object files (in case anything is ever built outside kernel/build/)
*.o

# Editor / OS cruft
.DS_Store
*.swp
.vscode/
